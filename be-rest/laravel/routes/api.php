<?php

/**
 * REST API routes.
 *
 * Endpoints per spec/21-app/00-rest-general/spec.md — placeholders only.
 * No handler logic yet; controllers will be wired in a later step.
 */

use Illuminate\Support\Facades\Route;

// Route::apiResource('students', StudentController::class);
// Route::post('file-upload', [FileController::class, 'upload']);
// Route::get('file-serve/{id}', [FileController::class, 'serve']);
