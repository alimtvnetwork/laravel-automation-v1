<?php

use Illuminate\Support\Facades\Route;

// API routes — endpoint logic will be added later per spec/21-app/00-rest-general/spec.md.
// Placeholder only. No handlers registered yet.

Route::prefix('v1')->group(function () {
    // Route::apiResource('students', \App\Http\Controllers\Api\StudentController::class);
    // Route::post('file-upload', [\App\Http\Controllers\Api\FileUploadController::class, 'store']);
    // Route::get('file-serve/{id}', [\App\Http\Controllers\Api\FileServeController::class, 'show']);
});
